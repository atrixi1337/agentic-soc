# Agentic SOC Lab — Environment Configuration
# Copy this file to lab.env and fill in your values.
# The cold_start.sh script will source lab.env if it exists.

# --- Wazuh (default lab values shown — change for your deployment) ---
WAZUH_INDEXER_URL=https://localhost:9200
WAZUH_INDEXER_USER=admin
WAZUH_INDEXER_PASS=SecretPassword
WAZUH_MANAGER_URL=https://localhost:55000
WAZUH_MANAGER_USER=wazuh-wui
WAZUH_MANAGER_PASS=MyS3cr37P450r.*-

# --- SOC-Victim VM (default lab values) ---
VM_USER=victim
VM_PASSWORD=m.m.m.m

# --- OpenRouter API key (REQUIRED for Tier-3 LLM and YARAKIN) ---
# Get a free key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# --- VirusTotal API key (OPTIONAL — for n8n Tier-3 enrichment) ---
# Get a free key at https://www.virustotal.com/gui/my-apikey
VT_API_KEY=

# --- YARAKIN LLM model (default is free tier) ---
YARAKIN_LLM_MODEL=cohere/north-mini-code:free

# --- Network ---
LAB_HOST_IP=192.168.10.101
