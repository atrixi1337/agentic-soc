# Wazuh indexer internal_users.yml (default admin user).
# Passwords are injected at runtime via env / bootstrap; this is the seed doc.
_meta:
  type: internalusers
  config_version: 2
admin:
  hash: "$2y$12$ihAibQLW8rvTE/ks.Q4sUe2oOBRnRAVYzXtWSzdwK9tW3o3/.pG8K"
  reserved: true
  backend_roles:
    - "admin"
  description: "Admin user"
